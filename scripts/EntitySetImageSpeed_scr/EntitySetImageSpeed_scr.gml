/// @description EntitySetImageSpeed_scr(entity, imageSpeed)
/// @param entity
/// @param  imageSpeed

var entity = argument0;
var imgSpd = argument1;

with(entity)
{
  image_speed = imgSpd;
  m_imageSpeedRaw = imgSpd;
}
